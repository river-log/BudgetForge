import { useState } from "react";
import { Plus } from "lucide-react";
import { Button, Card, CardContent, CardHeader, CardTitle } from "../ui";
function AddDebt({ addDebt }) { const [name, setName] = useState(""); const [balance, setBalance] = useState(""); const [apr, setApr] = useState(""); const [minimum, setMinimum] = useState(""); function submit(event) { event.preventDefault(); if (!name.trim() || !balance || !apr || !minimum) return; addDebt({ id: Date.now(), name: name.trim(), balance: Number(balance), apr: Number(apr), minimum: Number(minimum) }); setName(""); setBalance(""); setApr(""); setMinimum(""); } return <Card className="widget" padding="lg"><CardHeader><div><CardTitle>Add debt</CardTitle><p className="bf-card__description">Add a balance to your payoff workspace.</p></div></CardHeader><CardContent><form className="workspace-form" onSubmit={submit}><label>Debt name<input required maxLength="80" value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Credit card" /></label><label>Current balance<input type="number" required min="0.01" step="0.01" inputMode="decimal" value={balance} onChange={(event) => setBalance(event.target.value)} placeholder="0" /></label><label>APR<input type="number" required min="0" max="100" step="0.01" inputMode="decimal" value={apr} onChange={(event) => setApr(event.target.value)} placeholder="0" /></label><label>Minimum payment<input type="number" required min="0.01" step="0.01" inputMode="decimal" value={minimum} onChange={(event) => setMinimum(event.target.value)} placeholder="0" /></label><Button type="submit" leftIcon={<Plus size={16} />}>Add debt</Button></form></CardContent></Card>; }
export default AddDebt;
