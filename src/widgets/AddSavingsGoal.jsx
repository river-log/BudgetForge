import { useState } from "react";
import { Plus } from "lucide-react";
import { Button, Card, CardContent, CardHeader, CardTitle } from "../ui";
function AddSavingsGoal({ addGoal }) { const [name, setName] = useState(""); const [target, setTarget] = useState(""); const [saved, setSaved] = useState(""); function submit(event) { event.preventDefault(); if (!name.trim() || !target) return; addGoal({ id: Date.now(), name: name.trim(), target: Number(target), saved: Number(saved) || 0 }); setName(""); setTarget(""); setSaved(""); } return <Card className="widget" padding="lg"><CardHeader><div><CardTitle>New savings goal</CardTitle><p className="bf-card__description">Create a target worth working toward.</p></div></CardHeader><CardContent><form className="workspace-form" onSubmit={submit}><label>Goal name<input required maxLength="80" value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Emergency fund" /></label><label>Target amount<input type="number" required min="0.01" step="0.01" inputMode="decimal" value={target} onChange={(event) => setTarget(event.target.value)} placeholder="0" /></label><label>Current saved<input type="number" min="0" step="0.01" inputMode="decimal" value={saved} onChange={(event) => setSaved(event.target.value)} placeholder="0" /></label><Button type="submit" leftIcon={<Plus size={16} />}>Add goal</Button></form></CardContent></Card>; }
export default AddSavingsGoal;
